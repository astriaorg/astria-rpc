syntax = 'proto3';

package execution.v1;

message DoBlockRequest {
  bytes header = 1;
  repeated bytes transactions = 2;
}

message DoBlockResponse {
  bytes breaking_change = 1;
}

service ExecutionService {
  rpc DoBlock(DoBlockRequest) returns (DoBlockResponse);
}
